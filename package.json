{
  "name": "blog-post",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc && cp -r src/index.html src/styles.css dist/",
    "start": "npx live-server dist"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "typescript": "^5.6.3"
  }
}
